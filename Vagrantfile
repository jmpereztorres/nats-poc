# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
  config.vm.box = "luisbelloch/mbda_fintech"
  config.vm.box_version = "20180705.1"
  config.vm.synced_folder ".", "/vagrant", type: "virtualbox"
end
